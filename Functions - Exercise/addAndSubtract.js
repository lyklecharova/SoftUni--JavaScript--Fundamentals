function addAndSubtract(one, two, three) {
    let sum = one + two;
    let res = sum - three;
    console.log(res)

    // let sumFirstAndSecond = sum(one, two);
    // let finalResult = substract(sumFirstAndSecond, three)
    // console.log(finalResult);

    // function sum(one, two) {
    //     return one + two;

    // }

    // function substract(sumFirstAndSecond, thirdNumber) {
    //     return sumFirstAndSecond - thirdNumber;
    // }

    // let sum = (one, two) => one + two;
    // let substract = (sumFirstAndSecond, thirdNumber) => sumFirstAndSecond - thirdNumber;

    // let sumFirstAndSecond = sum(one, two);
    // let finalResult = substract(sumFirstAndSecond, three);
    // console.log(finalResult);
}
